(function(){
'use strict';
const $=id=>document.getElementById(id);
const EMPLOYEE_KEY='inspectorEmployeeDirectoryV3';
const LEGACY_KEY='inspectorEmployeeDirectoryV2';
const norm=v=>String(v||'').trim().toLowerCase();
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function employees(){let local=[];try{local=JSON.parse(localStorage.getItem(EMPLOYEE_KEY)||localStorage.getItem(LEGACY_KEY)||'[]');}catch(e){}const base=Array.isArray(window.INSPECTOR_EMPLOYEE_DIRECTORY)?window.INSPECTOR_EMPLOYEE_DIRECTORY:[];const shared=Array.isArray(window.SHARED_EMPLOYEE_DIRECTORY)?window.SHARED_EMPLOYEE_DIRECTORY:[];const map=new Map();[...base,...shared,...(Array.isArray(local)?local:[])].forEach(x=>{const e=Array.isArray(x)?{name:x[0],employeeNumber:x[1],depot:x[2]||'',jobTitle:x[3]||''}:x;const name=String(e?.name||'').trim(),number=String(e?.employeeNumber||e?.['Employee Number']||'').trim();if(name&&number)map.set(number.replace(/^0+(?=\d)/,''),{name,employeeNumber:number,depot:String(e?.depot||''),jobTitle:String(e?.jobTitle||'')});});return [...map.values()];}
function style(){if($('checkEntryControlsCss'))return;const s=document.createElement('style');s.id='checkEntryControlsCss';s.textContent=`#itToggleEntry{width:100%;margin:0 0 12px;background:#17324b;border:1px solid #416783;color:#fff;border-radius:10px;padding:12px;font-weight:800;font-size:15px}#itManualEntry{display:none}#integratedTimingResult{display:none!important}.itDriverWrap{position:relative}.itDriverSuggestions{display:none;position:static;width:100%;max-height:240px;overflow-y:auto;background:#102638;border:1px solid #3e6d8d;border-top:0;border-radius:0 0 10px 10px;box-shadow:0 10px 22px rgba(0,0,0,.42);box-sizing:border-box}.itDriverSuggestions.show{display:block}.itDriverSuggestion{display:block;width:100%;padding:10px 12px;border:0;border-bottom:1px solid rgba(255,255,255,.08);background:transparent;color:#fff;text-align:left}.itDriverSuggestion b{display:block}.itDriverSuggestion small{opacity:.75}.jointInspectorField{grid-column:1/-1;padding:10px;border:1px solid #36586f;border-radius:11px;background:#102b40}.jointInspectorField>span{display:block;margin-bottom:6px;font-weight:800}.jointHelp{display:block;margin-top:6px;color:#b8c5ce;font-size:11px;line-height:1.4}`;document.head.appendChild(s);}
function setupInspectionToggle(){const section=$('checksheet'),btn=$('toggleCheckEntryBtn');if(!section||!btn)return;const formSections=()=>Array.from(section.querySelectorAll('.formSection')),saveRow=$('saveCheckSheetBtn')?.parentElement,targets=()=>[...formSections(),saveRow].filter(Boolean);const setOpen=open=>{targets().forEach(el=>{el.classList.toggle('hubForcedHidden',!open);el.style.removeProperty('display');});btn.textContent=open?'HIDE INSPECTION':'SUBMIT NEW INSPECTION';btn.setAttribute('aria-expanded',String(open));btn.dataset.entryOpen=open?'1':'0';};btn.onclick=e=>{e.preventDefault();e.stopImmediatePropagation();setOpen(btn.dataset.entryOpen!=='1');};if(btn.dataset.entryControlsReady!=='1'){btn.dataset.entryControlsReady='1';setOpen(false);}}
function setupTimingToggle(){const panel=$('integratedTimingPanel');if(!panel)return;let manual=$('itManualEntry');if(!manual){manual=document.createElement('div');manual.id='itManualEntry';const results=panel.querySelector('.itResultsWrap');const moving=[...panel.children].filter(el=>el!==results&&el.id!=='itToggleEntry');moving.forEach(el=>manual.appendChild(el));panel.insertBefore(manual,results||null);}let btn=$('itToggleEntry');if(!btn){btn=document.createElement('button');btn.id='itToggleEntry';btn.type='button';btn.className='btn';panel.insertBefore(btn,manual);}const setOpen=open=>{manual.style.display=open?'block':'none';btn.textContent=open?'HIDE TIMING CHECK':'SUBMIT NEW TIMING CHECK';btn.setAttribute('aria-expanded',String(open));btn.dataset.entryOpen=open?'1':'0';};btn.onclick=e=>{e.preventDefault();e.stopImmediatePropagation();setOpen(btn.dataset.entryOpen!=='1');};if(btn.dataset.entryControlsReady!=='1'){btn.dataset.entryControlsReady='1';setOpen(false);}const prompt=$('integratedTimingResult');if(prompt)prompt.remove();}
function setupTimingDriver(){const input=$('itDriver');if(!input||input.dataset.directoryReady==='1')return;input.dataset.directoryReady='1';input.placeholder='Driver name or employee number';input.autocomplete='off';const label=input.closest('.itFieldLabel')||input.parentElement;label?.classList.add('itDriverWrap');const list=document.createElement('div');list.id='itDriverSuggestions';list.className='itDriverSuggestions';input.insertAdjacentElement('afterend',list);const choose=e=>{input.value=e.name;input.dataset.employeeNumber=e.employeeNumber;list.classList.remove('show');list.innerHTML='';input.dispatchEvent(new Event('change',{bubbles:true}));};const render=()=>{const q=norm(input.value);if(!q){list.classList.remove('show');list.innerHTML='';return;}const rows=employees().filter(e=>norm(e.name).includes(q)||norm(e.employeeNumber).includes(q)).sort((a,b)=>{const as=norm(a.name).startsWith(q)?0:1,bs=norm(b.name).startsWith(q)?0:1;return as-bs||a.name.localeCompare(b.name);}).slice(0,20);list.innerHTML=rows.map((e,i)=>`<button type="button" class="itDriverSuggestion" data-i="${i}"><b>${esc(e.name)}</b><small>Employee ${esc(e.employeeNumber)}${e.depot?' • '+esc(e.depot):''}</small></button>`).join('');list.classList.toggle('show',rows.length>0);list.querySelectorAll('[data-i]').forEach(b=>b.onclick=()=>choose(rows[Number(b.dataset.i)]));};input.addEventListener('input',render);input.addEventListener('focus',render);input.addEventListener('keydown',e=>{if(e.key==='Enter'){const first=list.querySelector('[data-i]');if(first){e.preventDefault();first.click();}}});document.addEventListener('click',e=>{if(!label?.contains(e.target))list.classList.remove('show');});}
function setupJointInspector(){const sheet=$('checksheet');if(!sheet||$('csSecondInspector'))return;const grid=sheet.querySelector('.grid');if(!grid)return;const field=document.createElement('label');field.className='fieldLabel jointInspectorField';field.innerHTML='<span>Additional Inspector <small>(optional)</small></span><select class="field" id="csSecondInspector"><option value="">None — single inspector check</option><option>M Skinner</option><option>A Duncan</option><option>A Fairbairn</option><option value="Other">Other inspector…</option></select><input class="field" id="csSecondInspectorOther" placeholder="Enter additional inspector name" style="display:none;margin-top:7px"><small class="jointHelp">Use this when two inspectors check the same bus. One history record is saved, but both inspectors receive credit.</small>';grid.appendChild(field);$('csSecondInspector').onchange=()=>{$('csSecondInspectorOther').style.display=$('csSecondInspector').value==='Other'?'block':'none';};}
function additionalInspector(){const s=$('csSecondInspector');if(!s)return'';return s.value==='Other'?String($('csSecondInspectorOther').value||'').trim():String(s.value||'').trim();}
function resetJoint(){if($('csSecondInspector'))$('csSecondInspector').value='';if($('csSecondInspectorOther')){$('csSecondInspectorOther').value='';$('csSecondInspectorOther').style.display='none';}}
function installJointSave(){const btn=$('saveCheckSheetBtn');if(!btn||btn.dataset.jointReady==='1')return;btn.dataset.jointReady='1';btn.onclick=()=>{const second=additionalInspector(),primary=String(getInspector()||'').trim();if(second&&second.toLowerCase()===primary.toLowerCase()){alert('The additional inspector must be different from the logged-in inspector.');return;}if($('csSecondInspector').value==='Other'&&!second){alert('Please enter the additional inspector name.');return;}const row=[normaliseDateForSave($('csDate').value),normaliseTimeForSave(now()),primary,$('csDepot').value,$('csDriver').value,$('csService').value,$('csFleet').value,normaliseTimeForSave($('csTimeOn').value),$('csBoarding').value,$('csDestination').value,$('csNSA').value,$('csNSAFault').value,$('csNSANotes').value,$('csDriverReport').value,$('csDriverReason').value,second];cloudAppend('Inspections',row);cloudAppend('NSA Faults',[row[0],row[1],primary,row[3],row[6],row[5],row[4],row[10],row[11],row[12],row[8],row[9],'Inspector Check Sheet',second]);if(row[13]!=='No Driver Report')cloudAppend('Driver Reports',[row[0],row[1],primary,row[4],row[3],row[13],row[14],row[5],row[6],second]);clearCheckForm();resetJoint();setTimeout(loadCloud,1200);};}
function patchHistory(){if(typeof renderChecks!=='function'||renderChecks.jointWrapped)return;const original=renderChecks;window.renderChecks=function(){original();let rows=(cloud['Inspections']||[]).slice(1);const q=($('checkSearch').value||'').toLowerCase(),n=new Date();rows=rows.filter(r=>{const d=parseRowDate(r[0]);if(checkFilter==='today'&&!sameDay(d,n))return false;if(checkFilter==='week'&&!inThisWeek(d))return false;if(checkFilter==='month'&&!inThisMonth(d))return false;if(q&&!r.join(' ').toLowerCase().includes(q))return false;return true;}).reverse();document.querySelectorAll('#checkList .compactCheck').forEach((card,i)=>{const r=rows[i],second=String(r?.[15]||'').trim();if(!second)return;const sub=card.querySelector('.compactSub');if(sub)sub.innerHTML+=' • <b>Inspectors: '+esc(r[2])+' &amp; '+esc(second)+'</b>';const details=card.querySelector('.compactDetails');if(details)details.innerHTML='<b>Inspectors:</b> '+esc(r[2])+' &amp; '+esc(second)+'<br>'+details.innerHTML;});};window.renderChecks.jointWrapped=true;}
function managementStart(){const range=document.querySelector('[data-mg-range].selected')?.dataset.mgRange||'month',n=new Date();n.setHours(0,0,0,0);if(range==='today')return n;if(range==='week'){const d=n.getDay();n.setDate(n.getDate()-d+(d===0?-6:1));return n;}if(range==='month')return new Date(n.getFullYear(),n.getMonth(),1);return new Date(0);}
function patchStats(){const box=$('mgInspectorPerformance');if(!box||typeof cloud==='undefined')return;const counts={},start=managementStart();(cloud['Inspections']||[]).slice(1).filter(r=>parseRowDate(r[0])>=start).forEach(r=>{[r[2],r[15]].forEach(n=>{n=String(n||'').trim();if(n)counts[n]=(counts[n]||0)+1;});});box.querySelectorAll('.mgInspectorRow').forEach(row=>{const name=String(row.querySelector('strong')?.textContent||'').trim(),cells=row.querySelectorAll('span');if(cells[0]&&counts[name]!==undefined){cells[0].textContent=counts[name]+' checks';delete counts[name];}});Object.keys(counts).forEach(name=>{const row=document.createElement('div');row.className='mgInspectorRow';row.innerHTML='<strong>'+esc(name)+'</strong><span>'+counts[name]+' checks</span><span>0 advised</span><span>0 reported</span><span>0 timing</span><span>0 NSA</span>';box.appendChild(row);});}
function run(){style();setupInspectionToggle();setupTimingToggle();setupTimingDriver();setupJointInspector();installJointSave();patchHistory();patchStats();}
function init(){run();setTimeout(run,700);setTimeout(run,1800);setTimeout(run,3200);setInterval(patchStats,1800);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,2500));else setTimeout(init,2500);
})();