(function(){
'use strict';
const $=id=>document.getElementById(id);
function addStyle(){if($('homeStopRefreshCss'))return;const s=document.createElement('style');s.id='homeStopRefreshCss';s.textContent='.homeNearestStopCard{position:relative}.homeNearestHead{padding-right:58px!important}.homeNearestRefresh{position:absolute;right:10px;top:14px;z-index:5;width:36px;height:36px;border:1px solid #4e7792;border-radius:50%;background:#173d5c;color:#fff;font-size:23px;line-height:1;font-weight:800}.homeNearestRefresh:disabled{opacity:.6}.homeNearestRefresh.spinning{animation:homeStopRefreshSpin .8s linear infinite}@keyframes homeStopRefreshSpin{to{transform:rotate(360deg)}}';document.head.appendChild(s);}
async function refresh(btn){if(btn.disabled)return;btn.disabled=true;btn.classList.add('spinning');const name=$('homeNearestStopName'),meta=$('homeNearestStopMeta');if(name)name.textContent='Finding closest bus stop…';if(meta)meta.textContent='Checking your current location…';try{if(typeof window.inspectorFindNearbyStops!=='function')throw new Error('Location lookup unavailable');const stops=await window.inspectorFindNearbyStops(false);if(!stops||!stops.length)throw new Error('No nearby stop found');window.inspectorNearbyStops={...(window.inspectorNearbyStops||{}),stops:stops.slice(),updated:Date.now()};document.dispatchEvent(new CustomEvent('inspector-nearby-stops',{detail:window.inspectorNearbyStops}));}catch(e){if(name)name.textContent='Unable to refresh location';if(meta)meta.textContent='Check location access and try again.';}finally{btn.disabled=false;btn.classList.remove('spinning');}}
function addButton(){const card=$('homeNearestStopCard');if(!card||$('homeNearestRefresh'))return false;const b=document.createElement('button');b.type='button';b.id='homeNearestRefresh';b.className='homeNearestRefresh';b.title='Refresh closest bus stop';b.setAttribute('aria-label','Refresh closest bus stop');b.textContent='↻';b.onclick=e=>{e.preventDefault();e.stopPropagation();refresh(b);};card.appendChild(b);return true;}
function init(){addStyle();let n=0,t=setInterval(()=>{n++;if(addButton()||n>80)clearInterval(t);},200);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,1200));else setTimeout(init,1200);
})();